-- Debug Adapter Protocol

return {
    'mfussenegger/nvim-dap',

    -- NOTE: On Windows with LLDB installed, the Python 3.10 embedded package had to be
    -- extracted into the same directory as the lldb.exe.
}
